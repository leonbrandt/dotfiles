#!/bin/bash

# Create a symlink "~/.config/nitrogen/current" to the current wallpaper

nitrogen --head=0 --set-scaled ~/.config/nitrogen/current --save
nitrogen --head=1 --set-scaled ~/.config/nitrogen/current --save
nitrogen --head=2 --set-scaled ~/.config/nitrogen/current --save
nitrogen --head=3 --set-scaled ~/.config/nitrogen/current --save
